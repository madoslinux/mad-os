# madOS Network Configuration
# Use iwd for WiFi backend (better Wayland support)
[device]
wifi.backend=iwd

# Prevent conflicts with systemd-networkd
# NetworkManager handles all IP configuration
[main]
plugins=keyfile
dns=default

# IPv6 policy - let NetworkManager handle it per-connection
[connectivity]
interval=300
